{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "TaskFlow Project Tracker - Self Hosted",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-all": "npm install && cd client && npm install",
    "build-client": "cd client && npm run build",
    "setup": "npm install && cd client && npm install && npm run build && cd .. && node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
